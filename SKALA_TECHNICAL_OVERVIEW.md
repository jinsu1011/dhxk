# dhxk SKALA 소개 자료

## 한 문장 소개

dhxk는 한·영 전환을 깜빡하고 입력한 문자열을 두벌식 물리 키 위치로 복원해 주는 Swift 기반 macOS 메뉴 막대 앱이다.

## 해결하려는 문제

문서, 메신저, 브라우저에서 `dkssudgktpdy`처럼 잘못 입력하면 지우고 입력 소스를 바꿔 다시 쓰는 반복 비용이 발생한다. dhxk는 단어가 끝나는 시점에 현재 입력 소스와 물리 키를 함께 분석하고, 확신도가 높은 경우에만 올바른 문자열로 교체한다.

대표 검증 예시는 다음과 같다.

- `ghdrlfehd` → `홍길동`
- `dkssudgktpdy` → `안녕하세요`
- `EMldjTMrlsk` → `띄어쓰기나`
- `TMaus` → `쓰면`
- `ㅗ디ㅣㅐ` → `hello`

## 사용한 기술과 구현 역량

| 영역 | 기술 | 적용 내용 |
|---|---|---|
| macOS 앱 | Swift, AppKit | Dock을 차지하지 않는 메뉴 막대 백그라운드 앱과 최초 등록 창 |
| 전역 입력 | CoreGraphics `CGEventTap` | 키 코드 감지, 단어 경계 판단, 합성 이벤트 표식으로 재귀 방지 |
| 접근성 | Accessibility `AXUIElement` | 포커스된 일반 텍스트 필드 확인, 실제 텍스트 범위 교체, 되돌리기 |
| 입력 소스 | Carbon Text Input Source API | 현재 한글/영문 입력 소스 판별 |
| 한글 처리 | Unicode 조합/분해, 두벌식 규칙 | 초성·중성·종성, 겹받침, 복합 모음, Shift 및 Caps Lock 처리 |
| 판정 | 규칙 기반 Language Scoring | 문자 비율, 모음/자음 패턴, 로컬 사전, 전후 점수 차이와 보수적 임계값 |
| 보안 | Secure Input, AX 메타데이터 분류 | 암호·아이디·이메일·OTP, Terminal·IDE·원격 앱 제외 |
| 로컬 학습 | 인메모리 Correction History | 수락/되돌림을 현재 실행 중에만 반영하고 입력 원문을 저장하지 않음 |
| 파일럿 운영 | `URLSession`, Google Apps Script/Sheets | 동의 기반 최초 1회 캠퍼스·반·이름 등록, 서버 재검증 |
| 품질 | SwiftPM 테스트, Universal 2 | 순수 변환·판정·보안·등록 규칙 자동 검사, arm64/x86_64 빌드 |

## 동작 구조

```text
물리 키 이벤트 → 보안/제외 조건 → 입력 소스 확인 → 현재 단어 버퍼
     → 단어 경계 → 두벌식 양방향 변환 → 자연스러움 점수 비교
     → 제안 또는 고신뢰 자동교체 → 즉시 되돌리기
```

핵심 변환과 판정은 macOS API에 의존하지 않는 `HangulInputCore`로 분리해 테스트할 수 있다. 앱 계층만 이벤트 탭, 접근성, 메뉴, 텍스트 교체를 담당한다.

## 개인정보와 안전 설계

- 키 입력과 작성 문서는 네트워크나 파일로 전송·저장하지 않는다.
- 현재 단어만 메모리에 두고 앱 종료 시 폐기한다.
- 비밀번호·인증·계정 입력이 의심되거나 접근성 정보가 불확실하면 작동하지 않는다.
- 파일럿 등록은 별도의 동의 화면에서 캠퍼스·반·이름만 최소 수집한다.
- 등록 성공 여부만 Mac에 저장하며 개인정보는 운영자가 승인한 Sheet에서 제한적으로 관리한다.

## SKALA에서 설명하는 방법

발표는 다음 순서가 가장 이해하기 쉽다.

1. **문제:** 한·영 전환 실수 때문에 반복해서 지우고 다시 입력한다.
2. **데모:** TextEdit, Chrome/ChatGPT, 카카오톡에서 양방향 예시를 보여준다.
3. **기술:** 물리 키 코드와 입력 소스를 함께 사용하고 Unicode 한글을 직접 조합한다.
4. **차별점:** 외부 AI 서버 없이 로컬에서 빠르게 처리하며 민감 필드를 기본 차단한다.
5. **품질:** 자동 테스트, 보수적 자동수정, 즉시 되돌리기, Universal 2 배포를 보여준다.
6. **파일럿:** 캠퍼스·반별 사용자를 파악하되 입력 내용은 수집하지 않는 최소 등록 방식임을 설명한다.
7. **다음 단계:** 실제 사용자 오수정 피드백, 앱별 호환성 측정, 서명·공증, 업데이트 체계를 제시한다.

30초 소개 예시:

> dhxk는 한·영 전환 실수를 자동으로 복원하는 macOS 메뉴 막대 앱입니다. Swift와 macOS 접근성·이벤트 API를 사용해 키 입력을 기기 안에서만 처리하고, 두벌식 조합 규칙과 보수적 언어 점수로 오수정을 줄였습니다. 비밀번호와 인증 필드는 차단하며 즉시 되돌리기를 제공합니다. 현재 SKALA 내부 파일럿을 위한 Universal 2 배포와 사용자 등록 흐름까지 구현했습니다.

## 현재 한계와 출시 전 과제

- 앱/웹 편집기마다 접근성 구현이 달라 호환성 실기기 시험이 계속 필요하다.
- 규칙과 로컬 사전 기반이므로 문장 전체 문맥을 이해하는 모델은 아니다.
- SKALA 파일럿 전에 개인정보 처리 담당자, 문의 채널, 교육 종료일, Google Workspace 사용 승인을 확정해야 한다.
- 외부 Mac 배포 전에 Apple Developer Program 가입, Developer ID 서명, 공증, GitHub Release가 필요하다.
