# dhxk 0.3.2 SKALA 파일럿

한·영 전환을 깜빡하고 입력한 오타를 두벌식 자판 기준으로 되돌려 주는 macOS 메뉴 막대 앱이다.

```
dkssudgktpdyrlawlstndlqslekwkfqnxkremflqslek
→ 안녕하세요김진수입니다잘부탁드립니다
```

입력한 키의 **물리적 위치**를 현재 단어 단위로 확인한 뒤, 반대 언어의 두벌식 자판으로 변환했을 때 더 자연스러운 단어가 되면 수정하거나 변환을 제안한다.

이 버전은 SKALA 참여자 전용 **무료 ad-hoc Pre-release**다. Apple Developer ID 서명과 공증을 받지 않았으므로 macOS가 개발자를 확인할 수 없으며, 사용자가 최초 실행을 직접 승인해야 한다. **공증된 정식 배포판이 아니다.** SKALA 파일럿 참여자가 아니라면 설치하거나 등록 정보를 제출하지 않는다.

## 주요 기능

- 영문 키 위치 오입력을 두벌식 한글로 복원
- 한글 상태에서 입력한 영문 키 위치를 영어로 복원
- 혼합 대문자와 Caps Lock 입력 지원
- 제안 모드와 자동수정 모드
- `⌘⌥Z` 즉시 되돌리기
- Apple Silicon과 Intel을 지원하는 Universal 2 앱
- SKALA 파일럿 최초 실행 시 캠퍼스·반·이름 등록
- 실제 입력 단어와 작성 문서는 서버로 전송하지 않고 Mac 안에서 처리
- 비밀번호·로그인·인증번호 입력창에서는 변환하지 않음
- 포커스된 일반 텍스트 요소를 확인할 수 없으면 자동 감지를 차단
- Accessibility 전체 문서 대신 caret 앞 최대 256 UTF-16 범위만 확인
- 등록 중복 방지, 허용 필드 고정, 일일 500건 상한 적용

## 실제 입력으로 확인한 동작 범위

**"모든 앱에서 동작"은 보장하지 않는다.** macOS 앱마다 입력창의 접근성 구현이 다르고, 비밀번호·보안 입력창은 의도적으로 변환을 차단한다. 아래는 실제 물리 키 이벤트를 넣어 확인한 결과다.

| 대상 | 결과 | 근거 |
| --- | --- | --- |
| Chrome `<textarea>` | ✅ `안녕하세요 ` | AX 값 판독 |
| Chrome `<input type=text>` | ✅ `안녕하세요 ` | AX 값 판독 |
| Chrome `<input type=password>` | ✅ **변환 안 함** | `AXSecureTextField` 서브롤로 차단 |
| Chrome User ID 입력란 | ✅ **변환 안 함** | 원문 `dkssudgktpdy ` 유지 |
| Chrome OTP 입력란 | ✅ **변환 안 함** | 원문 유지 |
| Claude Desktop 입력창 | ⚠️ 변환됨, 구분자 공백 유실 | 아래 알려진 제약 참조 |
| Antigravity 2.5.0 입력창 | ✅ `안녕하세요 ` (공백 포함) | 화면으로 확인 |
| 클립보드 보존 | ✅ 복원됨 | 붙여넣기 전후 타입 목록과 SHA-256 동일 |

자동 검사: Core **321/321**, backend 등록 **18/18** 통과.

검증은 macOS 26.5.1 / Apple Silicon 한 대에서 수행했다. Intel 슬라이스는 바이너리에 포함되어 있고 Rosetta 기동까지 확인했으나, 실제 Intel Mac에서는 시험하지 않았다.

### 알려진 제약

- **Claude Desktop 입력창에서는 변환 후 공백이 입력되지 않는다.** 변환 자체는 정상이다. 경계 키(스페이스)는
  이벤트 탭이 삼키므로 붙여넣기 문자열에 구분자를 포함해 보내는데, ProseMirror 계열 편집기가 붙여넣은
  문자열 **끝의 공백을 정규화해 지운다.** 변환 후 스페이스를 한 번 더 누르면 된다.
  - 붙여넣기와 구분자를 분리해 보내는 방식도 시험했으나, Chromium이 클립보드를 **비동기로** 읽는 탓에
    구분자가 변환문보다 **앞에** 삽입되는 더 나쁜 결과(`U+00A0` + 변환문)가 나와 채택하지 않았다.
- **Antigravity는 접근성 값이 갱신되지 않는다.** 화면은 정상 변환되지만 접근성 API는 변환 전 문자열을
  계속 반환한다. 이 앱의 검증은 화면 확인을 근거로 한다.
- 위 표에 없는 앱은 **확인하지 않았다.** 동작할 수도, 하지 않을 수도 있다.
- Terminal, IDE, 원격 데스크톱은 기본 제외 대상이다.
- 판정은 단어 경계 단위다. 여러 단어로 된 문장 전체의 문맥 모델은 아직 포함하지 않는다.
- 키 코드 표는 US ANSI 기반 두벌식 문자 키를 대상으로 한다. Dvorak/사용자 정의 물리 배열은 후속 지원이 필요하다.

## 설치

1. Release 자산의 `dhxk-0.3.2-skala-universal-adhoc.zip`과 `.sha256` 파일을 받는다.
2. SHA-256이 `.sha256` 파일의 값과 같은지 확인한다.
3. ZIP을 풀고 `dhxk.app`을 `/Applications`로 옮긴다.
4. 앱을 한 번 실행해 macOS 차단 메시지를 확인한다.
5. 시스템 설정 → 개인정보 보호 및 보안에서 `그래도 열기`를 누르고 다시 실행한다.
6. 손쉬운 사용과 입력 모니터링 권한을 허용한 뒤 앱을 재시작한다.

```bash
shasum -a 256 ~/Downloads/dhxk-0.3.2-skala-universal-adhoc.zip
```

메뉴 막대의 `한↔영` → `권한` 항목이 `손쉬운 사용 ✓ / 입력 모니터링 ✓ / 이벤트 ✓`로 표시되면 준비가 끝난 것이다.

상세 절차는 [사용자 매뉴얼](https://github.com/jinsu1011/dhxk/blob/v0.3.2/USER_MANUAL.md)을 따른다.
최초 등록 전에 [개인정보 처리 안내](https://github.com/jinsu1011/dhxk/blob/v0.3.2/PRIVACY.md)를 확인한다.

## 보안과 제한

- 이 앱은 Apple이 공증한 배포본이 아니며 Gatekeeper에서 자동 승인되지 않는다. **자동 승인 실패가 정상이다.**
- GitHub의 공식 `jinsu1011/dhxk` Release와 SHA-256을 확인한 경우에만 수동으로 허용한다.
- 관리되는 Mac에서는 기관 보안 정책 때문에 `그래도 열기`가 표시되지 않을 수 있다.
- 실제 입력 단어와 작성 문서는 서버로 전송하지 않고 Mac 안에서 처리한다. 외부로 나가는 통신은
  최초 1회 SKALA 등록 요청뿐이며, 전송 항목은 캠퍼스·반·이름과 등록 시각·앱 버전·동의문 버전이다.
- 비밀번호·ID·OTP 등 인증 입력란은 역할·서브롤·메타데이터로 판별해 변환하지 않는다.
- 로그인·결제·인증 화면에서는 자동 감지를 끄는 것을 권장하며, AX 포커스 요소를 확인할 수 없으면
  앱이 자동으로 차단한다.
- ad-hoc 서명은 지정 요구사항이 cdhash에 고정된다. 나중에 새 버전으로 앱을 교체하면 macOS가 기존
  손쉬운 사용·입력 모니터링 승인을 폐기하므로, 목록에서 기존 항목을 `−`로 제거한 뒤 `+`로 다시 추가해야
  한다. 토글만 껐다 켜는 것으로는 해결되지 않는다.

## 빌드 정보

- 버전: `0.3.2 (6)`
- Bundle ID: `com.jinsu1011.dhxk`
- 최소 macOS: 13.0
- 아키텍처: `arm64`, `x86_64` (Universal 2)
- 서명: ad-hoc, **미공증**
- SHA-256: `4733fe1b4737c925bcc8dee74c2cc4f24296b739a287940d99a3d9af06cf3ca7`
