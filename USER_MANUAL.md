# dhxk 사용자 매뉴얼

## 1. 앱 소개

dhxk는 macOS에서 한·영 입력 상태를 착각해 입력한 단어를 두벌식 자판 기준으로 복원하는 메뉴 막대 앱이다.

예시:

- 영문 입력 상태에서 `ghdrlfehd` → `홍길동`
- 영문 입력 상태에서 `dkssudgktpdy` → `안녕하세요`
- 대문자가 섞인 `EMldjTMrlsk` → `띄어쓰기나`
- 대문자가 섞인 `TMaus` → `쓰면`
- 한글 입력 상태에서 `ㅗ디ㅣㅐ` → `hello`
- 한글 입력 상태에서 잘못 입력한 `world`, `document`, `project`, `report` 등의 키 위치를 영어로 복원

입력 변환은 Mac 안에서 수행하며 입력한 단어를 서버로 보내거나 파일에 저장하지 않는다. SKALA 내부 파일럿판은 최초 1회 사용자 등록 정보만 별도로 전송한다.

## 2. 시스템 요구사항

- macOS 13 이상
- 손쉬운 사용 권한
- 입력 모니터링 권한
- 두벌식 한글 입력 소스

현재 SKALA 파일럿 Pre-release는 Apple Silicon과 Intel을 모두 포함한 Universal 2 ZIP으로 제공한다.

## 3. 설치

1. 공식 GitHub Releases에서 최신 SKALA 파일럿 Pre-release ZIP을 다운로드한다.
2. Finder에서 ZIP 압축을 푼다.
3. `dhxk.app`을 `/Applications` 폴더로 복사한다.
4. Applications에서 앱을 한 번 실행해 macOS 차단 메시지를 확인한다.
5. 시스템 설정 → 개인정보 보호 및 보안으로 이동해 `그래도 열기`를 누른다.
6. 다시 나타나는 확인 창에서 `열기`를 누른다.
7. 화면 상단 메뉴 막대에서 `한↔영` 아이콘을 확인한다.

현재 무료 파일럿판은 Developer ID 서명과 Apple 공증을 받지 않은 ad-hoc 앱이다. 공식 GitHub 저장소와 SHA-256을 확인한 경우에만 위 예외를 허용한다. 관리되는 Mac에서는 기관 보안 정책 때문에 `그래도 열기`가 표시되지 않을 수 있다.

### SKALA 파일럿 최초 등록

1. 첫 실행 창에서 캠퍼스를 `판교캠퍼스`, `광주캠퍼스`, `울산캠퍼스` 중 선택한다.
2. `1반`부터 `10반` 중 본인의 반을 선택한다.
3. 이름을 입력한다.
4. 수집 목적, 항목, 보유기간, 동의 거부 안내를 읽고 동의한다.
5. `동의하고 시작하기`를 누른다.

서버에 정상 등록된 뒤에는 같은 Mac에서 다시 묻지 않는다. 앱 삭제 후 재설치해도 macOS 설정이 남아 있으면 다시 묻지 않지만, 앱 설정을 직접 삭제하거나 사용자 계정을 바꾸면 다시 등록할 수 있다. 네트워크 오류가 나면 정보는 등록되지 않으므로 연결을 확인하고 다시 시도한다.

등록되는 정보는 캠퍼스·반·이름과 운영에 필요한 등록 시각·앱 버전·동의문 버전이다. 키 입력, 작성 문서, 전화번호, 이메일은 등록하지 않는다.

앱을 Downloads나 iCloud 폴더에서 직접 실행하지 말고 Applications로 옮긴다. 경로가 바뀌면 macOS 권한을 다시 요구할 수 있다.

### 공식 파일 확인

- 소스 코드 자동 ZIP이 아니라 Release 자산의 `dhxk-0.3.2-skala-universal-adhoc.zip`을 받는다.
- 같은 Release의 `.sha256` 파일과 다운로드 ZIP의 SHA-256을 비교할 수 있다.
- 개발자를 확인할 수 없다는 경고는 현재 무료 파일럿판에서 예상되는 동작이다. `앱이 손상됨` 또는 악성코드 경고가 표시되면 실행하지 말고 공식 Release에서 다시 받는다.

```bash
shasum -a 256 ~/Downloads/dhxk-0.3.2-skala-universal-adhoc.zip
```

## 4. 권한 설정

메뉴 막대의 `한↔영`을 누르고 `권한` 항목을 선택한다.

시스템 설정에서 다음 두 항목을 허용한다.

1. 개인정보 보호 및 보안 → 손쉬운 사용 → `dhxk` 켜기
2. 개인정보 보호 및 보안 → 입력 모니터링 → `dhxk` 켜기

두 권한을 켠 다음 앱을 종료하고 다시 실행한다. 메뉴에 다음과 같이 표시되어야 한다.

```text
손쉬운 사용 ✓ / 입력 모니터링 ✓ / 이벤트 ✓
```

토글이 켜져 있는데 `✗`로 표시되면 **토글을 껐다 켜는 것으로는 해결되지 않는다.** 목록에서 `dhxk`를 선택해 `−`로 **제거한 뒤** `+`로 `/Applications/dhxk.app`을 다시 추가하고 켠 다음 앱을 재시작한다.

이 앱은 ad-hoc 서명이라 지정 요구사항이 cdhash에 고정된다. 앱을 새 버전으로 교체하면 cdhash가 바뀌므로 macOS가 기존 승인을 폐기하며, 예전 cdhash에 묶인 목록 항목은 켜져 있어도 동작하지 않는다. **나중에 버전을 올릴 때마다 위의 제거 후 재추가 절차가 필요하다.**

권한은 앱의 핵심 동작에만 사용한다.

- 손쉬운 사용: 일반 텍스트 입력 범위 교체와 되돌리기
- 입력 모니터링: 현재 단어의 물리 키 위치 판정

## 5. 기본 사용법

1. 메뉴 막대의 `한↔영`을 클릭한다.
2. `자동 감지 켜기`를 선택한다.
3. `동작 모드`에서 제안 모드 또는 자동수정 모드를 선택한다.
4. 평소처럼 입력한다.
5. 공백, Enter, Tab 또는 문장부호에서 현재 단어를 판정한다.

자동 감지 상태와 동작 모드는 앱을 재시작해도 유지된다. 실제 입력 단어와 수락·되돌림 학습 정보는 저장되지 않으며 앱 종료 시 사라진다.

### 제안 모드

- 앱이 후보를 발견해도 즉시 바꾸지 않는다.
- 메뉴에 나타나는 `제안 적용`을 사용자가 선택한다.
- 초기 사용과 중요한 문서 작성에 권장한다.

### 자동수정 모드

- 변환 결과의 신뢰도가 충분히 높을 때 자동으로 바꾼다.
- 애매한 단어는 그대로 둔다.
- 잘못 바뀌면 즉시 되돌릴 수 있다.

## 6. 되돌리기

자동 교체 직후 다음 중 하나를 사용한다.

- 전역 단축키 `⌘⌥Z`
- 메뉴 막대 → `최근 변환 되돌리기`

되돌린 단어는 앱을 종료하기 전까지 다시 자동수정하지 않는다. 커서를 옮기거나 다른 앱으로 전환하기 전에 즉시 되돌리는 것이 가장 안전하다.

## 7. 지원 앱

Accessibility 표준 텍스트 입력을 제공하는 대부분의 일반 앱에서 동작한다. 현재 확인·지원 대상으로 분류한 앱은 다음과 같다.

- TextEdit와 메모
- Chrome과 Safari의 일반 웹 편집기
- ChatGPT
- Claude
- 카카오톡
- Notion
- Pages와 Microsoft Word
- Slack 및 일반 문서·채팅 입력창

웹사이트나 앱이 자체 편집기를 사용하면 일부 기능이 제한될 수 있다. Terminal, IDE, 원격 데스크톱은 기본적으로 제외된다.

## 8. 자동으로 동작하지 않는 상황

보안을 위해 다음 상황에서는 감지 또는 교체하지 않는다.

- Secure Input 활성화
- 비밀번호·암호·보호된 입력 필드
- 로그인 ID·계정·이메일·OTP·PIN·인증번호로 식별된 필드
- Terminal, iTerm2, Xcode, VS Code, JetBrains IDE
- 원격 데스크톱과 TeamViewer
- Command·Control·Option이 포함된 단축키
- URL, 이메일, 경로, 숫자, 코드 형태
- 포커스된 일반 텍스트 요소를 Accessibility로 확인할 수 없는 입력창
- 사용자가 자동 감지를 끈 상태

접근성 정보를 전혀 제공하지 않는 입력창은 일반 ID 필드와 본문을 구분할 근거가 없으므로 자동 감지를 차단한다. 로그인·결제·인증 화면에서는 추가로 메뉴의 `자동 감지 끄기`를 사용하는 것이 가장 안전하다.

## 9. 로그인할 때 자동 실행

메뉴 막대에서 `로그인 시 실행 설정`을 선택한다. 등록되지 않으면 다음 위치에서 직접 추가한다.

시스템 설정 → 일반 → 로그인 항목 → 로그인 시 열기

목록에 `/Applications/dhxk.app`을 추가한다.

## 10. 문제 해결

### 메뉴 막대 아이콘이 없다

- Activity Monitor에서 dhxk가 실행 중인지 확인한다.
- Applications에서 앱을 다시 실행한다.
- 메뉴 막대 공간이 부족하면 Control Center 설정을 확인한다.

### 입력이 전혀 감지되지 않는다

- 메뉴가 `자동 감지 끄기`로 표시되는지 확인한다. 이 문구는 현재 감지가 켜졌다는 뜻이다.
- 세 권한이 모두 `✓`인지 확인한다.
- 입력 모니터링을 바꾼 뒤 앱을 재시작한다.

### TextEdit에서는 되지만 Chrome·ChatGPT에서 안 된다

- 손쉬운 사용 권한을 껐다 켠다.
- 대상 앱을 완전히 종료했다 다시 연다.
- dhxk도 재시작한다.
- 비밀번호·로그인·인증 필드가 아닌 일반 본문 입력창인지 확인한다.

### `ㅗ디ㅣㅐ`는 되지만 다른 영어가 안 된다

- 한글 입력 소스가 두벌식인지 확인한다.
- Shift나 Caps Lock이 섞인 영문 키도 지원하지만, 실제 의도가 대문자인 정상 영어라면 제안 모드에서 먼저 확인한다.
- 단어 뒤에 공백 또는 Enter를 입력한다.
- 세 글자 미만의 짧고 애매한 후보는 보수적으로 제외될 수 있다.
- URL, 이메일, 파일명, 코드 조각은 의도적으로 제외된다.

### 잘못 자동수정된다

- 바로 `⌘⌥Z`를 누른다.
- 제안 모드로 변경한다.
- 특정 앱에서 계속 문제가 나면 해당 앱을 제외 목록에 추가한다.

```bash
defaults write com.jinsu1011.dhxk ExcludedBundleIDs \
  -array com.example.Application
```

## 11. 업데이트

1. 공식 GitHub Release에서 새 파일럿 버전을 다운로드하고 SHA-256을 확인한다.
2. 앱을 종료한다.
3. 새 앱을 `/Applications`의 기존 앱 위에 복사한다.
4. 실행 후 권한 상태를 확인한다.

현재 ad-hoc 파일럿 빌드는 코드 요구사항이 버전마다 달라질 수 있어 업데이트할 때 권한을 다시 요구할 수 있다.

## 12. 삭제

1. 메뉴에서 앱을 종료한다.
2. `/Applications/dhxk.app`을 휴지통으로 옮긴다.
3. 시스템 설정의 손쉬운 사용과 입력 모니터링 목록에서 남은 항목을 제거한다.
4. 로그인 항목에 등록했다면 함께 제거한다.

앱은 입력 문자열을 파일에 저장하지 않는다. 환경설정까지 삭제하려면 터미널에서 다음을 실행할 수 있다.

```bash
defaults delete com.jinsu1011.dhxk
```

이 명령은 제외 앱과 앱 설정을 삭제하므로 필요한 경우에만 사용한다.

## 13. 개인정보와 보안

- 입력 단어는 메모리에만 일시 보관한다.
- 입력 내용을 외부 서버나 API로 전송하지 않는다.
- 실제 입력 문자열을 기본 로그에 남기지 않는다.
- 시스템 맞춤법 사전 조회도 Mac 내부에서 처리한다.
- 합성 입력 이벤트에 내부 marker를 붙여 재귀 교체를 막는다.

문제 신고 시 실제 비밀번호, 인증번호, 개인 대화 내용을 첨부하지 않는다. 앱 버전, macOS 버전, 대상 앱 이름, 권한 상태와 재현용 가상 단어만 제공한다.

전체 개인정보 처리 내용은 `PRIVACY.md`를 확인한다.

## 14. 지원 요청 양식

공개 GitHub 저장소의 Issues에서 다음 형식으로 신고한다.

```text
dhxk 버전:
macOS 버전:
Mac 종류: Apple Silicon / Intel
대상 앱과 버전:
권한 상태: 손쉬운 사용 / 입력 모니터링 / 이벤트
동작 모드: 제안 / 자동수정
재현용 가상 입력:
기대한 결과:
실제 결과:
```

실제 비밀번호, 이메일 주소, 인증번호, 업무 문서 또는 개인 대화는 첨부하지 않는다. 공개 배포 전 README와 개인정보 처리방침에 실제 지원 URL을 넣어야 한다.
